# Container Preliminaries

## Build context
.devcontainer/
├── devcontainer.json  <-- The main configuration file
├── Dockerfile         <-- To build a custom, pre-configured image
└── docker-compose.yml <-- To orchestrate your app and database
